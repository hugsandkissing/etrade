# Credentials and OAuth tokens — never commit these
.env
tokens.json

# Python
__pycache__/
*.pyc
.venv/
venv/
dashboard.html
challenge/.ledger.lock

# Swagger Engine runtime state (append-only locally; never commit live state)
swagger_state/
